query CategoryFinder__SearchCategories($term: String!) {
  category_finder(args: { search_text: $term }) {
    category_id
    category_name
    sub_category_id
    sub_category_name
    values
  }
}
